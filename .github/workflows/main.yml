on:
  push:
    branches:
    - main
jobs:
  run_tests:
    name: Run tests
    runs-on: ubuntu-22.04
    steps:
      - name: Install Meson Build System
        run: |
          sudo apt update
          sudo apt install meson
