[build-system]
requires = ["setuptools", "Cython"]
build-backend = "setuptools.build_meta"

[project]
name = "sst_tests"
version = "v0.0.1"

[tool.setuptools]
ext-modules = [{name = "gfgb_py", sources = ["src/*.pyx"], depends = ["src/gfgb.pxd"]}]
